import { css } from 'emotion'

export const floatingMenuButton = css({
    bottom: 10,
    right: 10,
    position: 'absolute !important' as any,
    height: '38px!important',
    width: '38px!important',
    backgroundColor: 'var(--chrome-primary-color) !important'
})