import { css } from 'emotion'

export const actionButton = css({
    margin: '0 5px 0 0',
    '&:hover': {
        cursor: 'pointer'
    }
})

export const primary = css({
    color: '#3367d6',
    textTransform: 'uppercase'
})