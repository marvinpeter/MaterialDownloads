import * as React from 'react'
import * as ReactDOM from 'react-dom'
import Options from './components/options'

document.addEventListener('DOMContentLoaded', () => ReactDOM.render(<Options />, document.getElementById('options')))